[package]
name = "bootycall-oled"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
tokio = { workspace = true }
sysinfo = { workspace = true }
bootycall-core = { path = "../bootycall-core" }
bootycall-log = { workspace = true }
tracing = { workspace = true }
thiserror = { workspace = true }
libc = { workspace = true }
ab_glyph = { workspace = true }
gpiocdev = "0.8.0"

[features]
# Run the render loop off-device, dumping frames to PNG via the dependency-free
# `png` module. Adds no new dependencies (no windowing/SDL crate), so the
# default build and the shipped binary are unaffected.
sim = []

[lints]
workspace = true
