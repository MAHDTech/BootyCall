[package]
name = "bootycall-tftp"
version = "0.1.0"
edition = "2024"

[dependencies]
bootycall-core = { path = "../bootycall-core" }
tokio = { workspace = true }
tracing = { workspace = true }

[dev-dependencies]
tempfile = { workspace = true }
